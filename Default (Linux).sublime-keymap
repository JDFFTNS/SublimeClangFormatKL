[
    // TODO: figure out how to match multiple scopes.
    { "keys": ["ctrl+option+c"], "command": "clang_format",
        "context":
        [
            {"key": "selector", "operator": "equal", "operand": "source.kl"}
        ]
    }
]